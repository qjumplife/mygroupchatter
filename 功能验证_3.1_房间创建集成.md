# 功能验证 3.1 - 房间创建集成

## 📋 验证目标

验证权限控制系统已成功集成到房间创建流程中。

## ✅ 已完成的工作

1. ✅ 在 `useRoom.ts` 中导入权限控制服务
2. ✅ 添加权限控制状态管理
3. ✅ 扩展 `roomContextValue` 包含权限控制字段
4. ✅ 添加权限系统初始化 useEffect

## 🧪 验证步骤

### 方式 1：浏览器控制台验证（推荐）

1. **启动开发服务器**
   ```bash
   npm start
   ```

2. **创建私有房间**
   - 访问 `http://localhost:3000`
   - 点击 "Create private room"
   - 设置房间密码（例如：test123）
   - 点击 "Join"

3. **打开浏览器控制台**（F12）

4. **验证权限系统初始化**
   
   在控制台输入以下代码：
   ```javascript
   // 获取 RoomContext
   const roomContext = window.__REACT_DEVTOOLS_GLOBAL_HOOK__
   
   // 或者直接检查 localStorage
   const keys = Object.keys(localStorage).filter(k => k.startsWith('chitchatter_creator_'))
   console.log('创建者信息:', keys)
   
   // 应该看到类似 "chitchatter_creator_<roomId>" 的键
   ```

5. **验证 CreatorInfo 存储**
   ```javascript
   // 查看存储的数据
   const roomId = window.location.pathname.split('/').pop()
   const creatorKey = `chitchatter_creator_${roomId}`
   const stored = localStorage.getItem(creatorKey)
   
   if (stored) {
     console.log('✅ CreatorInfo 已存储')
     const data = JSON.parse(stored)
     console.log('存储结构:', Object.keys(data)) // 应该有 iv 和 data
   } else {
     console.log('❌ CreatorInfo 未找到')
   }
   ```

### 方式 2：React DevTools 验证

1. **安装 React DevTools**（如果还没有）
   - Chrome: https://chrome.google.com/webstore/detail/react-developer-tools/fmkadmapgofadopljbjfkapdkoienihi

2. **打开 React DevTools**
   - F12 → Components 标签

3. **查找 Room 组件**
   - 在组件树中找到 `Room` 或 `RoomCore`
   - 查看 hooks 部分

4. **验证状态**
   应该看到以下状态：
   - `isRoomCreator`: true
   - `authorityPackage`: { version: 1, timestamp: "...", keyset: [], signature: "..." }
   - `contentKey`: CryptoKey 对象
   - `creatorPublicKey`: CryptoKey 对象
   - `creatorPrivateKey`: CryptoKey 对象

### 方式 3：代码断点验证

1. **在 useRoom.ts 中设置断点**
   - 打开 `src/components/Room/useRoom.ts`
   - 在第 ~580 行（权限系统初始化的 useEffect）设置断点

2. **创建私有房间**
   - 按照方式 1 的步骤创建房间

3. **检查变量**
   当断点触发时，检查：
   - `authority` 对象是否正确创建
   - `authority.authorityPackage` 是否有效
   - `authority.contentKey` 是否存在

## ✅ 验证清单

完成以下检查后，功能验证通过：

- [ ] 创建私有房间时无控制台错误
- [ ] localStorage 中存在 `chitchatter_creator_<roomId>` 键
- [ ] 存储的数据包含 `iv` 和 `data` 字段
- [ ] React DevTools 显示 `isRoomCreator: true`
- [ ] React DevTools 显示 `authorityPackage` 对象
- [ ] React DevTools 显示 `contentKey` 对象
- [ ] 重新加载页面后身份恢复（`isRoomCreator` 仍为 true）

## 🐛 常见问题

### Q1: localStorage 中没有 creator 键
**可能原因**：
- 房间不是私有的（只有私有房间才启用权限控制）
- 密码未设置
- 初始化失败

**解决方法**：
- 确保创建的是 "private room"
- 检查浏览器控制台是否有错误
- 查看 Network 标签是否有请求失败

### Q2: isRoomCreator 为 false
**可能原因**：
- 权限系统初始化失败
- useEffect 未执行

**解决方法**：
- 检查 `isPrivate` 是否为 true
- 检查控制台错误信息
- 确认 password 参数已传递

### Q3: 重新加载后身份丢失
**可能原因**：
- 密码不一致
- localStorage 被清除
- 恢复逻辑失败

**解决方法**：
- 使用相同的密码重新加入
- 检查 `restoreCreatorAuthority` 是否被调用
- 查看控制台错误

## 📊 预期结果

### 成功的控制台输出：
```
✅ CreatorInfo 已存储
存储结构: ['iv', 'data']
```

### 成功的 React DevTools 状态：
```
hooks:
  - isRoomCreator: true
  - authorityPackage: Object
    - version: 1
    - timestamp: "2025-01-XX..."
    - keyset: []
    - signature: "base64string..."
  - contentKey: CryptoKey
  - creatorPublicKey: CryptoKey
  - creatorPrivateKey: CryptoKey
```

## 🎯 下一步

验证通过后，继续：
- **功能 3.2**: 集成邀请管理 UI

---

**验证状态**: ⏳ 待验证
**最后更新**: 2025-01-XX
