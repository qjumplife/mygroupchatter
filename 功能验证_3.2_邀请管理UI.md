# 功能验证 3.2 - 邀请管理 UI 集成

## 📋 验证目标

验证邀请管理面板已成功集成到 Room 组件中，创建者可以生成、查看、吊销邀请密钥。

## ✅ 已完成的工作

1. ✅ 在 Room.tsx 中导入 InviteManagerPanel
2. ✅ 为创建者显示邀请管理面板
3. ✅ 面板位于调试面板和聊天记录之间

## 🧪 验证步骤

### 1. 查看邀请管理面板

刷新页面（F5），应该看到：
- 🔐 房间权限系统调试（蓝色面板）
- 🔑 邀请密钥管理（灰色面板）← **新增**
- 聊天记录区域

### 2. 生成邀请密钥

在 "🔑 邀请密钥管理" 面板中：

1. **设置有效期**
   - 默认 24 小时
   - 可以修改（1-168 小时）

2. **点击 "生成密钥" 按钮**
   - 等待约 0.5 秒（密钥派生需要时间）
   - 应该显示黄色警告框

3. **查看生成的密钥**
   - 格式：`XXXX-XXXX-XXXX-XXXX`
   - 例如：`A7B3-K9M2-P4T6-X8Z1`

4. **复制密钥**
   - 点击 "复制密钥" 按钮
   - 应该弹出 "密钥已复制到剪贴板"

5. **关闭密钥显示**
   - 点击 "关闭" 按钮
   - 黄色框消失

### 3. 查看密钥列表

生成密钥后，应该在 "密钥列表" 中看到：
- ✅ 状态：可用（绿色）
- 创建时间
- 过期时间
- "吊销" 按钮

### 4. 吊销密钥

1. **点击 "吊销" 按钮**
   - 弹出确认对话框
   - 点击 "确定"

2. **查看状态变化**
   - 状态变为：已吊销（红色）
   - "吊销" 按钮消失

### 5. 生成多个密钥

1. 生成 2-3 个密钥
2. 查看统计信息更新：
   - 可用：X
   - 已使用：0
   - 已过期：0

### 6. 测试过期密钥清理

1. **生成过期密钥**
   - 设置有效期为 1 小时
   - 修改代码临时测试：将 `ttlHours` 改为 `-1`（负数表示已过期）
   - 或者等待密钥过期

2. **点击 "清理过期" 按钮**
   - 应该弹出 "已清理 X 个过期密钥"
   - 过期密钥状态变为：已过期（灰色）

## ✅ 验证清单

- [ ] 看到 "🔑 邀请密钥管理" 面板
- [ ] 可以设置有效期
- [ ] 点击 "生成密钥" 后显示密钥
- [ ] 密钥格式正确（XXXX-XXXX-XXXX-XXXX）
- [ ] 可以复制密钥
- [ ] 密钥出现在列表中
- [ ] 统计信息正确显示
- [ ] 可以吊销密钥
- [ ] 吊销后状态变为红色
- [ ] 可以生成多个密钥
- [ ] "清理过期" 功能正常

## 📊 预期界面

```
┌─────────────────────────────────────┐
│ 🔐 房间权限系统调试                │
│ ✅ isRoomCreator: true              │
│ ✅ authorityPackage: v1, 1 keys     │← 注意密钥数量变化
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 🔑 邀请密钥管理                     │
│                                     │
│ 可用: 1  已使用: 0  已过期: 0      │
│                                     │
│ 生成新密钥                          │
│ 有效期（小时）: [24] [生成密钥]    │
│                                     │
│ 密钥列表 (1)                        │
│ ┌─────────────────────────────┐   │
│ │ ✅ 可用 #1                  │   │
│ │ 创建: 2025-01-XX 20:00:00   │   │
│ │ 过期: 2025-01-XX 20:00:00   │   │
│ │                    [吊销]   │   │
│ └─────────────────────────────┘   │
└─────────────────────────────────────┘

[聊天记录区域...]
```

## 🐛 常见问题

### Q1: 生成密钥很慢
**正常现象**：密钥派生使用 PBKDF2（100,000 次迭代），需要约 0.5 秒

### Q2: 点击生成密钥没反应
**检查**：
- 浏览器控制台是否有错误
- isRoomCreator 是否为 true
- contentKey 是否存在

### Q3: 密钥列表不更新
**检查**：
- React DevTools 中 authorityPackage.keyset 是否增加
- 是否有控制台错误

## 🎯 下一步

验证通过后，继续：
- **功能 3.3**: 测试完整的邀请流程（需要两个浏览器窗口）

---

**验证状态**: ⏳ 待验证
**最后更新**: 2025-01-XX
