# 功能验证 3.4 - P2P 验证流程集成

## 📋 验证目标

验证完整的 P2P 验证流程，新用户可以使用邀请密钥加入房间并获得 Content Key。

## ✅ 已完成的工作

1. ✅ 添加 JOIN_REQUEST 消息处理
2. ✅ 添加 JOIN_RESPONSE 消息处理
3. ✅ 实现新用户验证请求
4. ✅ 实现老用户验证逻辑
5. ✅ 实现 Content Key 分发
6. ✅ 实现密钥状态更新

## 🧪 验证步骤（需要两个浏览器窗口）

### 准备工作

1. **打开两个浏览器窗口**
   - 窗口 A：创建者
   - 窗口 B：新用户

### 步骤 1：创建者创建房间并生成密钥

**窗口 A（创建者）**：

1. 访问 http://localhost:3000
2. 点击 "Create private room"
3. 设置密码：`test123`
4. 点击 "Join"
5. 在邀请管理面板中点击 "生成密钥"
6. 复制生成的密钥（例如：`ABCD-EFGH-IJKL-MNOP`）

### 步骤 2：新用户加入房间

**窗口 B（新用户）**：

1. 复制窗口 A 的房间 URL
2. 在窗口 B 中访问该 URL
3. 输入相同的密码：`test123`
4. 点击 "Join"
5. **应该弹出提示框**："请输入邀请密钥："
6. 输入刚才复制的密钥
7. 点击确定

**预期结果**：
- ✅ 显示 "验证成功！"
- ✅ 窗口 A 显示 "Someone has joined the room"
- ✅ 密钥状态变为 "已使用"

### 步骤 3：测试消息加密

**窗口 A（创建者）**：
- 发送消息：`Hello from creator!`

**窗口 B（新用户）**：
- 应该看到明文：`Hello from creator!`
- 发送消息：`Hello from new user!`

**窗口 A（创建者）**：
- 应该看到明文：`Hello from new user!`

### 步骤 4：测试未验证用户

**打开窗口 C（未验证用户）**：

1. 访问相同的房间 URL
2. 输入密码：`test123`
3. 点击 "Join"
4. **弹出密钥提示时，点击取消**

**预期结果**：
- ❌ 无法发送消息
- ❌ 看到的消息是：`[🔒 加密消息 - 需要验证]`

## ✅ 验证清单

### 基本功能
- [ ] 新用户加入时弹出密钥输入框
- [ ] 输入正确密钥后验证成功
- [ ] 输入错误密钥后验证失败
- [ ] 密钥状态更新为"已使用"
- [ ] 已验证用户可以发送消息
- [ ] 已验证用户可以看到明文消息

### 安全功能
- [ ] 未验证用户无法发送消息
- [ ] 未验证用户看到占位符
- [ ] 已使用的密钥无法再次使用
- [ ] 过期密钥无法使用
- [ ] 被吊销的密钥无法使用

### 边界情况
- [ ] 取消输入密钥后无法发送消息
- [ ] 多个用户同时加入
- [ ] 创建者离开后重新加入

## 📊 预期界面

### 窗口 A（创建者）
```
🔑 邀请密钥管理
可用: 0  已使用: 1  已过期: 0

密钥列表 (1)
┌─────────────────────────────┐
│ 已使用 #1                   │
│ 创建: 2025-01-XX 20:00:00   │
│ 过期: 2025-01-XX 20:00:00   │
│ 使用者: user-id-xxx         │
└─────────────────────────────┘

[聊天记录]
你: Hello from creator!
用户B: Hello from new user!
```

### 窗口 B（新用户，已验证）
```
[聊天记录]
创建者: Hello from creator!
你: Hello from new user!
```

### 窗口 C（未验证用户）
```
[聊天记录]
创建者: [🔒 加密消息 - 需要验证]
用户B: [🔒 加密消息 - 需要验证]

[消息输入框]
需要验证才能发送消息 ❌
```

## 🔍 调试技巧

### 查看网络消息

在浏览器控制台（F12）中：

```javascript
// 查看当前状态
console.log('isRoomCreator:', /* 从 React DevTools 查看 */)
console.log('contentKey:', /* 从 React DevTools 查看 */)
console.log('authorityPackage:', /* 从 React DevTools 查看 */)
```

### 查看密钥状态

在创建者窗口的邀请管理面板中：
- 查看密钥列表
- 确认状态变化

## 🐛 常见问题

### Q1: 没有弹出密钥输入框
**原因**: 
- 不是私有房间
- 已经是创建者
- 已经有 contentKey

**解决**: 确保是新用户加入私有房间

### Q2: 输入密钥后没反应
**原因**: 
- 创建者不在线
- 网络连接问题
- 密钥格式错误

**解决**: 
- 确保创建者窗口在线
- 检查控制台错误
- 确认密钥格式正确

### Q3: 验证成功但无法发送消息
**原因**: contentKey 未正确设置

**解决**: 
- 检查 React DevTools 中的 contentKey
- 刷新页面重试

### Q4: 消息显示乱码
**原因**: 加密/解密密钥不匹配

**解决**: 
- 确保使用正确的邀请密钥
- 检查 contentKey 是否一致

## 🎯 成功标准

### 必须通过
- ✅ 新用户可以使用密钥加入
- ✅ 已验证用户可以收发消息
- ✅ 未验证用户看到占位符
- ✅ 密钥状态正确更新

### 应该通过
- ✅ 错误密钥被拒绝
- ✅ 已使用密钥无法重复使用
- ✅ 多用户场景正常

---

**验证状态**: ⏳ 待验证
**最后更新**: 2025-01-XX
