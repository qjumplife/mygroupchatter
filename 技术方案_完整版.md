# Chitchatter 权限控制系统 - 完整技术方案 v2.0

## 📋 一、需求与实现方式总览

| 需求 ID | 需求描述 | 实现方式 | 优先级 | 难度 |
|---------|---------|---------|--------|------|
| R1 | 唯一房间管理员 | Ed25519 密钥对 + 本地标记 | P0 | ⭐⭐ |
| R2 | 邀请密钥生成 | Ki + Hash(Ki) + L 数据结构 | P0 | ⭐ |
| R3 | 密钥单次使用 | status 锁定 + usedBy 记录 | P0 | ⭐⭐⭐ |
| R4 | 密钥过期（TTL） | expiration 字段 + 定期检查 | P0 | ⭐ |
| R5 | 密钥吊销 | status=REVOKED + 广播 | P1 | ⭐⭐ |
| R6 | 新用户验证加入 | Hash(Ki) 验证 + Content Key 分发 | P0 | ⭐⭐⭐ |
| R7 | 真实安全隔离 | 双层加密（WebRTC + Content Key） | P0 | ⭐⭐⭐ |
| R8 | 防调试工具窃取 | 无 Content Key = 密文 | P0 | ⭐⭐ |
| R9 | 管理员离线验证 | L 分布式存储 + 独立验证 | P0 | ⭐⭐ |
| R10 | 节点重上线同步 | HELLO_SYNC + L_DATA + 版本比较 | P1 | ⭐⭐⭐ |
| R11 | 全网状态一致性 | version + timestamp + 签名 | P0 | ⭐⭐⭐ |
| R12 | 权威性保证 | Ed25519 签名验证 | P0 | ⭐⭐ |
| R13 | Pcreator 身份持久化 | 加密存储到 localStorage | P0 | ⭐⭐ |
| R14 | Content Key 分发 | Ki 加密存储在 L 中 | P0 | ⭐⭐⭐ |
| R15 | 已验证用户重连 | localStorage 缓存 | P1 | ⭐ |
| R16 | 历史消息加密 | Content Key 加密存储 | P1 | ⭐⭐ |
| R17 | 竞态条件处理 | 乐观锁 + 冲突检测 | P2 | ⭐⭐⭐ |

---

## 🏗️ 二、核心架构设计

### 2.1 系统分层

```
┌─────────────────────────────────────┐
│         UI Layer (React)            │
│  InviteManagerPanel / Room.tsx      │
└─────────────────┬───────────────────┘
                  │
┌─────────────────▼───────────────────┐
│      Business Logic Layer           │
│  useInvitationKeys / useRoom        │
└─────────────────┬───────────────────┘
                  │
┌─────────────────▼───────────────────┐
│      State Management Layer         │
│  RoomContext / SettingsContext      │
└─────────────────┬───────────────────┘
                  │
┌─────────────────▼───────────────────┐
│      Service Layer                  │
│  Encryption / Serialization         │
└─────────────────┬───────────────────┘
                  │
┌─────────────────▼───────────────────┐
│      Network Layer                  │
│  PeerRoom (WebRTC P2P)              │
└─────────────────────────────────────┘
```

---

## 📊 三、核心数据结构

### 3.1 权威密钥列表 L (Authority Package)

```typescript
interface AuthorityPackage {
  version: number                    // 单调递增版本号
  timestamp: string                  // UTC ISO 8601
  keyset: InviteKeyRecord[]         // 密钥记录数组
  signature: string                  // Ed25519 签名（Base64）
}

interface InviteKeyRecord {
  hash: string                       // SHA-256(Ki) Base64
  expiration: string                 // UTC ISO 8601
  status: KeyStatus                  // ACTIVE | USED | REVOKED | EXPIRED
  usedBy: string | null             // Peer ID
  createdAt: string                  // UTC ISO 8601
  encryptedContentKey: EncryptedData // 🔑 用 Ki 加密的 Content Key
}

type KeyStatus = "ACTIVE" | "USED" | "REVOKED" | "EXPIRED"

interface EncryptedData {
  iv: string                         // Base64 编码的 IV
  ciphertext: string                 // Base64 编码的密文
}
```

### 3.2 邀请密钥 Ki

```typescript
interface InviteKey {
  key: string                        // 原始密钥（如 "S7X-93F-G5T-HF8"）
  ttl: string                        // 过期时间
}
```

### 3.3 创建者信息（持久化）

```typescript
interface CreatorInfo {
  roomId: string
  role: "creator"
  privateKey: string                 // Ed25519 私钥（Base64）
  publicKey: string                  // Ed25519 公钥（Base64）
  contentKey: string                 // Content Key（Base64）
  createdAt: string
}
```

### 3.4 已验证用户信息（持久化）

```typescript
interface VerifiedUserInfo {
  roomId: string
  contentKey: string                 // Content Key（Base64）
  verifiedAt: number                 // 时间戳
  inviteKeyHash: string              // 用于审计
}
```

### 3.5 Peer 对象扩展

```typescript
interface ExtendedPeer {
  peerId: string
  connection: RTCPeerConnection
  dataChannel: RTCDataChannel
  
  // 🆕 新增字段
  role: "creator" | "member" | "pending" | "rejected"
  verifiedAt?: number
  inviteKeyHash?: string
  hasContentKey: boolean
}
```

---

## 🔐 四、加密方案详细设计

### 4.1 双层加密架构

```
原始消息
    ↓
[第一层] Content Key 加密（AES-GCM-256）
    ↓
密文 A
    ↓
[第二层] WebRTC E2EE 加密（原有机制）
    ↓
密文 B（传输）
    ↓
[第二层] WebRTC E2EE 解密
    ↓
密文 A
    ↓
[第一层] Content Key 解密（需要验证）
    ↓
原始消息
```

### 4.2 Content Key 的生成与分发

```typescript
// 1. Pcreator 创建房间时生成
const contentKey = await crypto.subtle.generateKey(
  { name: "AES-GCM", length: 256 },
  true,
  ["encrypt", "decrypt"]
)

// 2. 生成 Ki 时，用 Ki 加密 Content Key
const kiDerivedKey = await deriveKeyFromKi(Ki)
const encryptedContentKey = await encryptContentKey(contentKey, kiDerivedKey)

// 3. 存储在 L.keyset[i].encryptedContentKey

// 4. Pnew 验证通过后，收到 encryptedContentKey
const kiDerivedKey = await deriveKeyFromKi(Ki)
const contentKey = await decryptContentKey(encryptedContentKey, kiDerivedKey)
```

### 4.3 Ki 派生密钥算法

```typescript
async function deriveKeyFromKi(Ki: string): Promise<CryptoKey> {
  const kiBytes = new TextEncoder().encode(Ki)
  
  const keyMaterial = await crypto.subtle.importKey(
    "raw",
    kiBytes,
    "PBKDF2",
    false,
    ["deriveKey"]
  )
  
  return await crypto.subtle.deriveKey(
    {
      name: "PBKDF2",
      salt: new TextEncoder().encode("chitchatter-content-key-v1"),
      iterations: 100000,
      hash: "SHA-256"
    },
    keyMaterial,
    { name: "AES-GCM", length: 256 },
    false,
    ["encrypt", "decrypt"]
  )
}
```

---

## 🔄 五、核心流程设计

### 5.1 房间创建流程

```
用户点击"创建房间"
    ↓
生成 Room ID + Password（原有逻辑）
    ↓
🆕 生成 Ed25519 密钥对（签名用）
    ↓
🆕 生成 Content Key（内容加密用）
    ↓
🆕 初始化空的 L（version=1）
    ↓
🆕 加密存储 CreatorInfo 到 localStorage
    ↓
显示房间界面 + 邀请管理面板
```

**验证点**：
- localStorage 中存在 `room_${roomId}_creator`
- L.version === 1
- L.keyset.length === 0

---

### 5.2 生成邀请密钥流程

```
Pcreator 点击"生成邀请密钥"
    ↓
输入 TTL（可选，默认 24 小时）
    ↓
生成随机 Ki（16 字符）
    ↓
计算 Hash(Ki) = SHA-256(Ki)
    ↓
派生 kiDerivedKey = deriveKeyFromKi(Ki)
    ↓
加密 encryptedContentKey = encrypt(contentKey, kiDerivedKey)
    ↓
添加到 L.keyset：
  {
    hash: Hash(Ki),
    expiration: now + TTL,
    status: "ACTIVE",
    usedBy: null,
    encryptedContentKey: encryptedContentKey
  }
    ↓
L.version++
    ↓
签名 L.signature = sign(L, privateKey)
    ↓
广播 L 到所有在线 Peer
    ↓
UI 显示 Ki（供复制分享）
```

**验证点**：
- L.keyset 新增一条记录
- L.version 递增
- L.signature 有效
- UI 显示 Ki

---

### 5.3 新用户加入验证流程

```
Pnew 输入 Room ID + Password + Ki
    ↓
建立 WebRTC 连接（原有逻辑）
    ↓
peer.role = "pending"
peer.hasContentKey = false
    ↓
计算 Hash(Ki)
    ↓
发送 JOIN_REQUEST {hashKi} 到任一 Pold
    ↓
─────────────────────────────────
Pold 收到 JOIN_REQUEST
    ↓
在 L.keyset 中查找 record (record.hash === hashKi)
    ↓
验证：
  ✓ record 存在？
  ✓ record.status === "ACTIVE"？
  ✓ record.expiration > now？
  ✓ L.signature 有效？
    ↓
验证通过：
  - record.status = "USED"
  - record.usedBy = peer.peerId
  - L.version++
  - 签名并广播 L
  - 发送 JOIN_RESPONSE {result: "ALLOW", encryptedContentKey}
    ↓
验证失败：
  - 发送 JOIN_RESPONSE {result: "DENY", reason}
  - 断开连接
    ↓
─────────────────────────────────
Pnew 收到 JOIN_RESPONSE
    ↓
if result === "ALLOW":
  - 派生 kiDerivedKey = deriveKeyFromKi(Ki)
  - 解密 contentKey = decrypt(encryptedContentKey, kiDerivedKey)
  - 存储 contentKey
  - peer.hasContentKey = true
  - 存储到 localStorage（用于重连）
  - 解密历史消息
  - 进入房间
else:
  - 显示错误原因
  - 断开连接
```

**验证点**：
- Pnew 端：hasContentKey === true
- Pold 端：L.keyset 中对应记录 status === "USED"
- Pnew 可以看到解密后的消息

---

### 5.4 消息加密/解密流程

```
发送消息：
  原始文本
    ↓
  if hasContentKey:
    加密 ciphertext = encrypt(contentKey, text)
  else:
    拒绝发送
    ↓
  通过 WebRTC 发送（自动二次加密）

接收消息：
  收到 WebRTC 消息（自动解密）
    ↓
  if hasContentKey:
    解密 plaintext = decrypt(contentKey, ciphertext)
    显示 plaintext
  else:
    显示 "[加密消息，需要验证]"
```

**验证点**：
- 未验证用户看到 "[加密消息，需要验证]"
- 已验证用户看到明文
- 调试工具中看到的是密文

---

### 5.5 密钥吊销流程

```
Pcreator 在 UI 选择密钥
    ↓
点击"吊销"
    ↓
找到 L.keyset 中对应记录
    ↓
record.status = "REVOKED"
    ↓
L.version++
    ↓
签名 L.signature = sign(L, privateKey)
    ↓
广播 L 到所有在线 Peer
    ↓
UI 更新状态显示
```

**验证点**：
- L.keyset 中对应记录 status === "REVOKED"
- 其他节点收到更新后，该 Ki 无法再验证通过

---

### 5.6 节点重上线同步流程

```
Pold 重新进入房间
    ↓
建立 WebRTC 连接
    ↓
检查 localStorage 是否有 VerifiedUserInfo
    ↓
if 有：
  - 恢复 contentKey
  - hasContentKey = true
  - role = "member"
    ↓
发送 HELLO_SYNC {version_local, timestamp_local}
    ↓
收到 SYNC_STATUS {version_remote, timestamp_remote}
    ↓
if version_remote > version_local:
  - 发送 GET_L {want_version}
  - 收到 L_DATA {L_full}
  - 验证 L.signature
  - 替换本地 L
  - 广播 SYNC_ACK
    ↓
进入正常通信
```

**验证点**：
- 本地 L.version 与远程一致
- 重连后可以正常收发消息

---

### 5.7 Pcreator 身份恢复流程

```
Pcreator 重新进入房间
    ↓
输入 Room ID + Password
    ↓
检查 localStorage 是否有 CreatorInfo
    ↓
if 有：
  - 用 Password 派生解密密钥
  - 解密 CreatorInfo
  - 恢复 privateKey, publicKey, contentKey
  - role = "creator"
  - 显示邀请管理面板
else:
  - role = "member"（普通用户）
```

**验证点**：
- Pcreator 重连后仍能看到邀请管理面板
- 可以生成新密钥、吊销密钥

---

## 📡 六、P2P 消息协议

### 6.1 消息类型定义

```typescript
// 加入验证
interface JoinRequestMessage {
  type: "JOIN_REQUEST"
  hashKi: string                     // Base64
  peerId: string
}

interface JoinResponseMessage {
  type: "JOIN_RESPONSE"
  result: "ALLOW" | "DENY"
  reason?: string                    // 拒绝原因
  encryptedContentKey?: EncryptedData // 验证通过时返回
}

// L 同步
interface HelloSyncMessage {
  type: "HELLO_SYNC"
  version_local: number
  timestamp_local: string
  peerId: string
}

interface SyncStatusMessage {
  type: "SYNC_STATUS"
  version_remote: number
  timestamp_remote: string
}

interface GetLMessage {
  type: "GET_L"
  want_version: number
}

interface LDataMessage {
  type: "L_DATA"
  L: AuthorityPackage
}

// 本地更新通知
interface LocalUpdateMessage {
  type: "LOCAL_UPDATE"
  update: {
    hash: string
    status: KeyStatus
    usedBy: string | null
  }
}
```

---

## 🗂️ 七、文件改造清单

### 7.1 核心服务层

| 文件 | 改造内容 | 优先级 |
|------|---------|--------|
| `src/services/Encryption/Encryption.ts` | 新增 Content Key 管理、双层加密/解密 | P0 |
| `src/services/Serialization/Serialization.ts` | 新增消息类型定义 | P0 |
| `src/services/Settings/Settings.ts` | 新增 CreatorInfo/VerifiedUserInfo 持久化 | P0 |

### 7.2 网络层

| 文件 | 改造内容 | 优先级 |
|------|---------|--------|
| `src/lib/PeerRoom/PeerRoom.ts` | 新增消息处理器、L 同步逻辑、Peer 状态管理 | P0 |

### 7.3 状态管理层

| 文件 | 改造内容 | 优先级 |
|------|---------|--------|
| `src/contexts/RoomContext.ts` | 新增 L 存储、contentKey 管理 | P0 |
| `src/contexts/SettingsContext.ts` | 扩展配置项 | P1 |

### 7.4 业务逻辑层

| 文件 | 改造内容 | 优先级 |
|------|---------|--------|
| `src/hooks/useRoom.ts` | 集成验证流程、消息加密/解密 | P0 |
| `src/hooks/useInvitationKeys.ts` | 新增邀请密钥管理逻辑 | P0 |

### 7.5 UI 层

| 文件 | 改造内容 | 优先级 |
|------|---------|--------|
| `src/components/Room/Room.tsx` | 集成验证 UI、显示加密状态 | P0 |
| `src/components/Room/InviteManagerPanel.tsx` | 邀请管理面板 | P0 |
| `src/components/Room/GenerateKeyButton.tsx` | 生成密钥按钮 | P0 |

---

## 🎯 八、功能实现顺序

### Phase 1：基础设施（2-3 天）

**功能 1.1：数据结构与加密工具**
- 定义 L, Ki, CreatorInfo 等数据结构
- 实现 Ed25519 签名/验证
- 实现 SHA-256 哈希
- 实现 Ki 派生密钥
- 实现 Content Key 加密/解密

**验证点**：
```typescript
// 测试代码
const Ki = "TEST-KEY-1234"
const hash = await sha256(Ki)
const derived = await deriveKeyFromKi(Ki)
const encrypted = await encryptContentKey(contentKey, derived)
const decrypted = await decryptContentKey(encrypted, derived)
assert(decrypted === contentKey)
```

---

**功能 1.2：Pcreator 创建房间**
- 生成 Ed25519 密钥对
- 生成 Content Key
- 初始化 L
- 加密存储 CreatorInfo

**验证点**：
- localStorage 中存在 `room_${roomId}_creator`
- 可以解密并恢复 CreatorInfo
- L.version === 1

---

### Phase 2：核心验证流程（3-4 天）

**功能 2.1：生成邀请密钥**
- UI 输入 TTL
- 生成 Ki
- 计算 Hash(Ki)
- 加密 Content Key
- 添加到 L
- 签名并广播

**验证点**：
- UI 显示生成的 Ki
- L.keyset 新增记录
- L.signature 有效

---

**功能 2.2：新用户验证加入**
- Pnew 输入 Ki
- 发送 JOIN_REQUEST
- Pold 验证逻辑
- 返回 encryptedContentKey
- Pnew 解密获得 contentKey

**验证点**：
- Pnew 端 hasContentKey === true
- Pold 端记录 status === "USED"
- 控制台日志显示验证成功

---

**功能 2.3：双层加密消息**
- 发送消息时用 contentKey 加密
- 接收消息时用 contentKey 解密
- 未验证用户看到密文占位符

**验证点**：
- 已验证用户看到明文
- 未验证用户看到 "[加密消息]"
- 浏览器调试工具中看到密文

---

### Phase 3：高级功能（2-3 天）

**功能 3.1：密钥吊销**
- UI 吊销按钮
- 修改 status
- 签名并广播

**验证点**：
- 吊销后该 Ki 无法验证通过
- 其他节点同步更新

---

**功能 3.2：节点重上线同步**
- HELLO_SYNC 消息
- 版本比较
- GET_L / L_DATA
- 替换本地 L

**验证点**：
- 重连后 L.version 与远程一致
- 可以正常收发消息

---

**功能 3.3：已验证用户重连**
- localStorage 缓存 contentKey
- 重连时恢复
- 跳过验证流程

**验证点**：
- 重连后直接进入房间
- 无需重新输入 Ki

---

### Phase 4：优化与测试（1-2 天）

**功能 4.1：竞态条件处理**
- 乐观锁
- 冲突检测
- 版本回滚

**功能 4.2：历史消息加密**
- 存储时加密
- 读取时解密

**功能 4.3：UI 优化**
- 密钥状态显示
- 过期提示
- 错误处理

---

## 🧪 九、测试策略

### 9.1 单元测试

```typescript
// 加密工具测试
describe("Encryption Utils", () => {
  test("deriveKeyFromKi", async () => {
    const Ki = "TEST-KEY"
    const key = await deriveKeyFromKi(Ki)
    expect(key).toBeDefined()
  })
  
  test("encryptContentKey / decryptContentKey", async () => {
    const contentKey = await generateContentKey()
    const Ki = "TEST-KEY"
    const encrypted = await encryptContentKey(contentKey, Ki)
    const decrypted = await decryptContentKey(encrypted, Ki)
    expect(decrypted).toEqual(contentKey)
  })
})

// 签名验证测试
describe("Signature", () => {
  test("sign and verify L", async () => {
    const keypair = await generateKeypair()
    const L = { version: 1, keyset: [], timestamp: new Date().toISOString() }
    const signature = await signL(L, keypair.privateKey)
    const valid = await verifyL(L, signature, keypair.publicKey)
    expect(valid).toBe(true)
  })
})
```

### 9.2 集成测试

```typescript
// 完整验证流程测试
describe("Join Verification Flow", () => {
  test("Pnew joins with valid Ki", async () => {
    // 1. Pcreator 创建房间
    const room = await createRoom()
    
    // 2. 生成 Ki
    const Ki = await room.generateInviteKey()
    
    // 3. Pnew 加入
    const pnew = await joinRoom(room.id, room.password, Ki)
    
    // 4. 验证
    expect(pnew.hasContentKey).toBe(true)
    expect(pnew.role).toBe("member")
  })
  
  test("Pnew joins with invalid Ki", async () => {
    const room = await createRoom()
    const invalidKi = "INVALID-KEY"
    
    await expect(joinRoom(room.id, room.password, invalidKi))
      .rejects.toThrow("INVALID")
  })
})
```

### 9.3 手动测试清单

- [ ] 创建房间，检查 localStorage
- [ ] 生成 3 个邀请密钥，检查 L.keyset
- [ ] 用第一个 Ki 加入，验证成功
- [ ] 用同一个 Ki 再次加入，验证失败（已使用）
- [ ] 吊销第二个 Ki，尝试加入，验证失败（已吊销）
- [ ] 等待第三个 Ki 过期，尝试加入，验证失败（已过期）
- [ ] 已验证用户发送消息，其他已验证用户可见
- [ ] 未验证用户看到 "[加密消息]"
- [ ] 关闭浏览器，重新打开，Pcreator 身份恢复
- [ ] 已验证用户重连，自动恢复 contentKey
- [ ] 打开浏览器调试工具，查看消息内容（应为密文）

---

## 📝 十、注意事项

### 10.1 安全注意事项

1. **私钥保护**：Ed25519 私钥必须加密存储
2. **Content Key 保护**：只在内存和加密存储中存在
3. **Ki 传输**：只传输 Hash(Ki)，不传输原始 Ki
4. **时钟同步**：TTL 依赖本地时钟，建议提示用户同步系统时间
5. **签名验证**：所有 L 更新必须验证签名

### 10.2 性能注意事项

1. **签名验证异步化**：避免阻塞 UI
2. **消息加密批量处理**：减少加密开销
3. **L 同步节流**：避免频繁广播
4. **IndexedDB 缓存**：减少重复计算

### 10.3 兼容性注意事项

1. **WebCrypto API**：需要 HTTPS 或 localhost
2. **IndexedDB**：需要浏览器支持
3. **Ed25519**：部分旧浏览器不支持，需 polyfill

---

## 🔮 十一、未来扩展

### 11.1 多级角色

```typescript
type Role = "creator" | "moderator" | "member" | "viewer"

interface RolePermissions {
  canGenerateKeys: boolean
  canRevokeKeys: boolean
  canKickUsers: boolean
  canSendMessages: boolean
}
```

### 11.2 密钥使用统计

```typescript
interface KeyUsageStats {
  totalGenerated: number
  totalUsed: number
  totalRevoked: number
  totalExpired: number
}
```

### 11.3 审计日志

```typescript
interface AuditLog {
  timestamp: string
  action: "KEY_GENERATED" | "KEY_USED" | "KEY_REVOKED" | "USER_JOINED"
  actor: string
  target?: string
  details: any
}
```

---

**文档版本**：v2.0  
**最后更新**：2025-01-XX  
**状态**：待实施
