# 功能 1.1：数据结构与加密工具 - 验证文档

## ✅ 功能概述
实现权限控制系统所需的所有加密工具函数。

## 📦 已实现的文件

### 1. 数据结构定义
- **文件**: `src/models/authority.ts`
- **内容**: 
  - `AuthorityPackage` - 权威密钥列表
  - `InviteKeyRecord` - 邀请密钥记录
  - `CreatorInfo` - 创建者信息
  - `VerifiedUserInfo` - 已验证用户信息

### 2. 加密工具函数
- **文件**: `src/services/Encryption/Encryption.ts`
- **新增函数**:
  - `generateEd25519KeyPair()` - 生成 Ed25519 密钥对
  - `generateContentKey()` - 生成 Content Key
  - `sha256()` - 计算 SHA-256 哈希
  - `deriveKeyFromKi()` - 从 Ki 派生密钥
  - `encryptContentKey()` - 加密 Content Key
  - `decryptContentKey()` - 解密 Content Key
  - `encryptMessage()` - 加密消息
  - `decryptMessage()` - 解密消息
  - `exportKey()` - 导出密钥
  - `importKey()` - 导入密钥
  - `signAuthorityPackage()` - 签名 L
  - `verifyAuthorityPackage()` - 验证 L 签名
  - `generateInviteKey()` - 生成邀请密钥

### 3. 单元测试
- **文件**: `src/services/Encryption/Encryption.test.ts`
- **测试覆盖**: 所有加密工具函数

### 4. 调试组件
- **文件**: `src/components/DebugEncryption/DebugEncryption.tsx`
- **用途**: 手动验证加密工具功能

---

## 🧪 验证步骤

### 方式 1：运行单元测试

```bash
cd C:\Users\tc\Documents\develop\lifebook\chitchatter\chitchatter
npm test -- Encryption.test.ts
```

**预期结果**：
- ✅ 所有测试通过
- ✅ 无错误或警告

---

### 方式 2：使用调试组件（推荐）

#### 步骤 1：临时添加调试路由

在 `src/pages/About/About.tsx` 中临时添加调试组件：

```typescript
import { DebugEncryption } from 'components/DebugEncryption'

// 在 About 组件中添加
<DebugEncryption />
```

#### 步骤 2：启动开发服务器

```bash
npm start
```

#### 步骤 3：访问调试页面

打开浏览器访问：`http://localhost:3000/about`

#### 步骤 4：点击"运行测试"按钮

**预期结果**：

```
[时间] 🚀 开始测试加密工具...
[时间] 📝 测试 1: 生成 Ed25519 密钥对
[时间] ✅ 公钥类型: public
[时间] ✅ 私钥类型: private
[时间] 📝 测试 2: 生成 Content Key
[时间] ✅ Content Key 类型: secret
[时间] 📝 测试 3: 生成邀请密钥
[时间] ✅ 邀请密钥: XXXX-XXXX-XXXX-XXXX
[时间] 📝 测试 4: 计算 SHA-256 哈希
[时间] ✅ 哈希值: ...
[时间] 📝 测试 5: 用 Ki 加密 Content Key
[时间] ✅ IV: ...
[时间] ✅ 密文: ...
[时间] 📝 测试 6: 用 Ki 解密 Content Key
[时间] ✅ 解密成功: true
[时间] 📝 测试 7: 用 Content Key 加密消息
[时间] ✅ 消息密文: ...
[时间] 📝 测试 8: 用 Content Key 解密消息
[时间] ✅ 解密消息: Hello, Chitchatter!
[时间] 📝 测试 9: 签名 Authority Package
[时间] ✅ 签名: ...
[时间] 📝 测试 10: 验证 Authority Package 签名
[时间] ✅ 签名有效: true
[时间] 🎉 所有测试通过！
```

---

### 方式 3：浏览器控制台手动测试

#### 步骤 1：打开浏览器控制台

按 F12 打开开发者工具

#### 步骤 2：导入加密工具

```javascript
import {
  generateEd25519KeyPair,
  generateContentKey,
  sha256,
  generateInviteKey,
  encryptContentKey,
  decryptContentKey,
} from './services/Encryption/Encryption'
```

#### 步骤 3：逐个测试

```javascript
// 测试 1: 生成密钥对
const keypair = await generateEd25519KeyPair()
console.log('公钥:', keypair.publicKey)
console.log('私钥:', keypair.privateKey)

// 测试 2: 生成 Content Key
const contentKey = await generateContentKey()
console.log('Content Key:', contentKey)

// 测试 3: 生成邀请密钥
const Ki = generateInviteKey()
console.log('邀请密钥:', Ki)

// 测试 4: 计算哈希
const hash = await sha256(Ki)
console.log('哈希:', hash)

// 测试 5: 加密 Content Key
const encrypted = await encryptContentKey(contentKey, Ki)
console.log('加密结果:', encrypted)

// 测试 6: 解密 Content Key
const decrypted = await decryptContentKey(encrypted, Ki)
console.log('解密结果:', decrypted)
```

---

## ✅ 验证清单

### 前提条件
- [x] 文件 `src/models/authority.ts` 已创建
- [x] 文件 `src/services/Encryption/Encryption.ts` 已更新
- [x] 文件 `src/services/Encryption/Encryption.test.ts` 已创建
- [x] 文件 `src/components/DebugEncryption/DebugEncryption.tsx` 已创建

### 功能验证
- [ ] Ed25519 密钥对生成成功
- [ ] Content Key 生成成功
- [ ] 邀请密钥格式正确（XXXX-XXXX-XXXX-XXXX）
- [ ] SHA-256 哈希计算正确
- [ ] Content Key 加密/解密成功
- [ ] 消息加密/解密成功
- [ ] Authority Package 签名/验证成功
- [ ] 错误的密钥无法解密（安全性验证）
- [ ] 修改后的 L 签名验证失败（完整性验证）

### 性能验证
- [ ] 所有加密操作在 100ms 内完成
- [ ] 无内存泄漏
- [ ] 无控制台错误

---

## 🐛 常见问题

### 问题 1：WebCrypto API 不可用
**原因**: 需要 HTTPS 或 localhost 环境

**解决**: 确保在 `http://localhost:3000` 或 HTTPS 环境下运行

### 问题 2：Ed25519 不支持
**原因**: 浏览器版本过旧

**解决**: 使用最新版 Chrome/Edge/Firefox

### 问题 3：测试超时
**原因**: PBKDF2 迭代次数过高

**解决**: 已设置为 100000 次，正常情况下应在 1 秒内完成

---

## 📊 性能基准

在现代浏览器中的预期性能：

| 操作 | 预期时间 |
|------|---------|
| 生成 Ed25519 密钥对 | < 50ms |
| 生成 Content Key | < 10ms |
| SHA-256 哈希 | < 5ms |
| Ki 派生密钥 | < 500ms |
| Content Key 加密 | < 50ms |
| Content Key 解密 | < 50ms |
| 消息加密 | < 10ms |
| 消息解密 | < 10ms |
| 签名 L | < 20ms |
| 验证签名 | < 20ms |

---

## ✅ 完成标准

当以下所有条件满足时，功能 1.1 验证通过：

1. ✅ 所有单元测试通过
2. ✅ 调试组件显示"所有测试通过"
3. ✅ 无控制台错误或警告
4. ✅ 所有验证清单项目勾选完成
5. ✅ 性能符合基准要求

---

## 🎯 下一步

功能 1.1 验证通过后，继续实现：

**功能 1.2：Pcreator 创建房间**
- 生成密钥对
- 初始化 L
- 加密存储 CreatorInfo
